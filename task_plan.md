# 07b02 代码回退计划

## 当前任务：RAG 性能与混合检索（2026-09-11）

此前回退记录仅为历史，不适用于当前已授权实现。
- [x] 请求追踪、在线预算与Embedding缓存
- [x] SDK常驻MCP并发适配器（5并发）
- [x] 条款切块及Milvus BM25/RRF实现（实际混合发布被兼容性检查阻止）
- [x] 维护、重建、回退操作与验收记录交付
- [x] 静态检查与可执行的手工组件联调；不新增测试代码
- [ ] 完整100题质量与80题5并发性能验收：缺题库/验收账号，Milvus RunAnalyzer不兼容，未执行迁移

验收：预热后未命中缓存的80个有效查询持续5并发，完整MCP调用全部<5s；外部服务未实测不能标为通过。保留已有本地改动，不自动迁移或覆盖生产数据。

## 目标

仅从当前项目移除 07b02 向量化与 Milvus 集成实现，保留 07b01 的上传、解析、切块、MinIO、ClamAV、权限和安全删除；新的 07b02 完整参考代码只在会话中输出。

## 安全边界

- 不使用 `git reset`、`git checkout`、`git clean`。
- 不覆盖或回退用户的其他本地改动。
- 不执行数据库反向迁移，不删除现有 MySQL 或 Milvus 数据。
- 共享文件只移除可明确归属于 07b02 的引用。

## 阶段

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | 核对工作区、暂存区和 07b01 基线 | complete |
| 2 | 回退 07b02 新增文件及共享文件引用 | complete |
| 3 | 清理配置、迁移和文档中的 07b02 内容 | complete |
| 4 | 验证 07b01 可导入、路由正常且无残留引用 | complete |
| 5 | 在会话输出符合需求文档的完整参考代码 | complete |

## 遇到的错误

| 错误 | 次数 | 处理 |
|---|---:|---|
| 原计划文件已不存在 | 1 | 按技能要求为本次回退重新建立最小计划文件 |
| 同一补丁同时删除并新增 `milvus_client.py` 被拒绝 | 1 | 整组补丁未应用；拆成删除与新增两个原子补丁 |
| 共享文件补丁中的 API 函数签名匹配多了字符 | 1 | 整组补丁未应用；改为逐文件小补丁 |
| 应用导入被 `.env` 中13个已回退字段阻止 | 1 | 仅删除这些07b02环境变量后重新验证 |

## 当前验收轮：2026-09-11
- 速度改进：缩短生成提示词、统一512输出上限，保持检索阈值不变；正在执行同题真实五并发复测。
- [x] 冻结原文与配置，制定100题（助理拟题，非用户预标注）
- [x] 真实MCP链路预热及80题持续5并发、缓存未命中测试：80/80<5秒
- [x] 100题回答逐条评阅，补充Top20证据检索：质量不通过，缺失版本样本不算通过
- [x] 汇总阶段耗时、失败样本与缺失权限/版本覆盖：artifacts/rag-acceptance-20260911/report.md

## 2026-09-11 速度改进
- 速度改进已完成：真实100题复测，80题冷缓存五并发无错误，P95 1309.58ms，最大2069.34ms；质量阈值问题仍待处理。

## 证据判断改进
- 已实现0.15候选下限、单次JSON生成及逐字来源校验，正在真实100题复测。

## 2026-09-11 证据判断实施结果
- 实现和复测完成；严格验收未通过：原23误拒答恢复，A40出现1次生成错误。后续待诊断其偶发原始输出，另需独立题库及真实机密/旧版本场景。
