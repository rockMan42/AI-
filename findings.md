# 07b02 回退发现

## 2026-09-11 RAG实现发现
- 工作区包含用户已实现的RAG与Schema目录迁移，以当前内容为基础修改。
- Hermes 0.19.0的MCP包装串行等待整个工具调用；SDK 1.26支持请求ID多路复用。
- Milvus目前仅有dense字段；需要新集合及BM25函数，并保留MySQL向量ID核验。
- 向量化Worker使用pytz.utc.now()，会在提交前报错；本次必须修正。
- 尚无已标注的100条问题集，不可编造真实质量或性能验收结果。

- 当前工作区包含大量既有暂存和未暂存改动，不能使用整文件 Git 回退。
- 07b02 与 `settings.py`、`main.py`、知识 API、文档服务和模型共享文件，需要逐项移除引用。
- 用户确认仅回退 07b02；数据库和 Milvus 中已有数据不在本次删除范围。
- 新的 07b02 实现只在会话输出，不写回当前项目。
- 原 07b01 需求明确要求 `kb_chunk.milvus_id` 和 `kb_chunk.is_active` 作为后续向量化预留字段，因此这两个模型字段必须保留。
- `kb_document.index_status`、`kb_index_task`、Embedding 客户端、索引 Worker、搜索路由和 Milvus Collection 操作属于 07b02，应移除。
- `document_cleanup.py` 和 `deleting/delete_failed` 属于此前确认保留的安全删除能力，不回退。
- Git 暂存区中的多个 07b01 文件只是空占位，无法作为自动恢复基线；共享文件必须手工做字段级回退。
- 当前百炼官方文档显示 `text-embedding-v3/v4` 同步接口单次最多10条，不是需求文档中的25条；新参考实现应使用10条并保留1024维和8192 Token限制。
- Milvus当前官方客户端支持自定义Schema、HNSW、AutoID Collection的全实体upsert，以及TLS证书参数；旧版本失活需要查询完整实体后修改 `is_active` 再upsert。

## 2026-09-11 RAG实施验证补充
- PyMilvus 3.0.1同步RPC重试复用timeout，在线查询改用可取消的AsyncMilvusClient；现有索引只读查询成功。
- 服务端Milvus v2.5.4的RunAnalyzer返回UNIMPLEMENTED，必须阻止混合发布并单独处理兼容性。
- FastMCP dict返回可能只有text content，项目适配器兼容解析text JSON与structuredContent。
- 原自定义PermissionError继承Exception造成接口捕获遗漏，已统一使用内置PermissionError，员工confidential实测HTTP403。
- SDK临时stdio五并发隔离、超时、断连恢复检查通过；这些结果不代表真实RAG达标。

## 2026-09-11 真实验收发现
- 当前Milvus2.5.27分析器正常；实际RAG_MAX_TOKENS=2048（原计划512），按现状计时。
- 80题耗时全<5秒但43题跳过生成，其中23本应有答案；不能以此宣称完整质量通过。
- 60题正确证据全部召回并进入补充重排Top5；标准上下班问题正式最高分0.196946被0.4门槛拒绝。
- 有答案最高分最低约0.1793，无答案最高分0.1849，分布重叠，不应未经独立校准直接下调阈值。
- 原Word即包含爱情提醒、最后扣款、霓人、不得低于C级及出差条款矛盾；生成还额外补出了21.75和无需额外条件等无依据规则。

## 2026-09-11 速度改进
- 两轮均37/80调用生成，缩短回答后尾部时延下降；P50略增，单轮对比包含模型服务波动。512只是上限，无法将收益单独归因于它；0.4误拒答23题依旧存在。

## 2026-09-11 证据判断实施结果
- 模型可能将条款号当来源ID，即使加S前缀仍发生；已使用原文字符串数组，来源完全由已授权候选中逐字匹配决定。按原文行输入减少换行转义/跨行拼接问题。r4仍1次生成错误，不能宣称全部达标；59条有回答不等于全部通过人工质量验收，A39缺少补卡原因说明。
