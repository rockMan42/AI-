# RAG 人工验收记录

## 最新：证据判断改进复测（2026-09-11）

已实现0.15召回下限、单次LLM证据判断、原文逐字定位引用。原23个误拒答全部恢复；60个有答案问题59条正常回答、A40出现1次生成阶段错误；20个无答案问题全部拒答。80题冷缓存持续五并发P95为2922.63ms、最大4205.04ms，79/80正常返回，**严格验收仍未通过**。不能用A40单次诊断成功替换正式失败。100题日志与身份匹配；真实机密及失效版本、独立人工题库仍未覆盖。

[当前完整复测报告](/Users/rockman/From-E/Project2026/python-project/ai-digital-employee/artifacts/rag-evidence-20260911-r4/report.md)。r1～r3的失败记录保留。未新增仓库测试代码。

## 历史：真实MCP验收（2026-09-11 19:59）

已执行100题；80个正常检索样本全部缓存未命中并在5秒内返回，最大3187.76ms，P95为2602.99ms。但60个有答案题中23个被误拒答，另10个回答含无直接依据的附加断言，整体质量不通过。机密文档及真实失效/删除版本样本缺失，相关隔离验收未覆盖。当前Milvus已为2.5.27，RunAnalyzer正常，早期兼容性阻塞已解除。

[完整报告与逐题记录](/Users/rockman/From-E/Project2026/python-project/ai-digital-employee/artifacts/rag-acceptance-20260911/report.md)。题库由助理依据原Word拟定并评阅，尚无业务人员签字；没有改动生产代码或知识文档。

## 早期组件检查（历史记录，以下环境与待办已被本轮部分更新）

日期：2026-09-11。结论：**代码与组件联调已完成，混合检索发布及最终性能/质量验收未通过完成条件，暂不能发布。** 本记录没有将模拟服务耗时作为真实 RAG 耗时。

## 环境与只读检查

| 项目 | 实际结果 |
|---|---|
| Python | 3.13，本项目 `.venv` |
| MCP / Hermes | 1.26.0 / 0.19.0 |
| PyMilvus / Milvus 服务端 | 3.0.1 / v2.5.4 |
| Redis | PING 成功；未清空缓存 |
| MySQL | 1 个 embedded 文档，32 个 embedded 且 active 的 internal 分块 |
| 现有向量 ID | 32/32 ID 存在，文档/版本/块号/权限/激活元数据全部对应 |
| 异步 Milvus 查询 | 对现有索引的只读稠密查询成功 |
| 中文分析器 | RunAnalyzer 返回 UNIMPLEMENTED，发布阻塞 |

一次零向量只读采样返回了 20 个当前 MySQL 中不存在的业务分块键；这些候选会被校验剔除。随后按现有 SQL 的真实 ID 检查，32/32 映射一致。零向量采样不代表真实问题的召回率，也没有据此删除索引数据。

## 已执行的手工组件联调

全部使用临时解释器命令，没有新增测试文件或测试脚本，没有运行数据迁移，没有调用真实回答模型，也没有向飞书发送消息。

| 检查 | 方法与结果 |
|---|---|
| 语法和接口导入 | 74 个 Python 文件 AST 检查通过；知识 API、飞书路由、适配器导入成功；git diff --check 通过 |
| Unicode 切分 | 长汉字/扩展汉字/emoji 文本拆分后可原样拼回，每块 ≤512 tokens |
| 条款与章节 | ≤512 tokens 的条款及例外保留在同块，同名的不同章节不合并 |
| 表格 | 超长行分块重复表头，正文片段无损，每块 ≤512 tokens |
| MCP 并发 | 真实 MCP SDK＋临时 stdio 服务，每次固定等待 200ms；5 个请求总计约 205ms，身份逐一对应。此服务不含 Embedding/Rerank/LLM，不是业务性能验收 |
| MCP 截止时间 | 50ms 预算调用 200ms 临时服务，返回错误，正常关闭连接和线程 |
| MCP 断连 | 临时子进程主动退出，在途请求失败；恢复任务重建连接，新请求成功，未重放原请求 |
| HTTP 权限 | ASGI 本地联调，注入员工身份请求 confidential，返回 403；不返回低置信度成功 |
| 维护入口 | 维护依赖函数手工调用返回 HTTP 503；未启动或停止实际业务 Worker |
| MySQL/RRF | 使用真实分块元数据做只读校验，伪造向量 ID/不存在版本被剔除；两路按原排名融合的顺序和去重符合预期，空权限不返回候选 |
| 查询缓存 | 内存缓存替身＋HTTP MockTransport；首尾空白归一后命中，条件与数字变化触发新 Embedding，键不包含明文，TTL 为 86400 |
| Redis 故障 | 缓存替身抛 OSError 后仍调用 Embedding。真实 Redis 停机故障未执行 |
| Embedding 故障 | MockTransport 抛 ReadTimeout，仅发起一次在线请求，没有自动重试 |
| 无候选 | 固定兜底，不调用生成模型，成功返回前调用日志提交 |
| 日志故障 | 本地注入提交失败，返回 RAGError；未写实际故障日志记录，未返回成功 |
| 截断回答 | 模拟 finish_reason=length，被明确拒绝 |
| BM25 发布保护 | 当前服务端检查失败时，没有进入集合创建/修改函数 |
| 重建入口保护 | 未提供维护状态和 --apply 时退出 1；帮助命令可用；未产生清单或变更数据库 |

## 尚未执行的最终验收

| 验收项 | 状态 / 缺少条件 |
|---|---|
| 原方案基线 | 未采集，需要原部署/原快照及固定人工题库 |
| 性能优化＋旧索引的 100 题 | 未执行，需要题库和验收账号 |
| 新切块纯向量对照 | 未执行，需要兼容环境、维护窗口、备份及重建 |
| BM25 混合检索质量 | 阻塞：中文分析器兼容性检查失败 |
| 60 个有答案题质量 | 待标注正确来源、必要条件、数字与例外 |
| 20 个无答案题 | 未执行完整题组 |
| 10 个权限＋10 个版本/删除状态题 | 未执行完整真实题组；现有数据仅有一个 internal 文档 |
| 80 个未命中缓存问题持续 5 并发，全部 <5s | 未执行，不能宣称达标 |
| 真实模型时延/配额、Redis/日志库故障 | 未做真实外部故障实验；本地模拟结果不能替代 |
| 备份恢复与新索引回退 | 仅交付操作说明和重建代码，尚未在维护窗口执行 |

最终统计：P50、P95、P99、最大耗时、错误率均为 **未测量**，不是 0。

后续按 `docs/rag-rollout.md` 执行。每阶段记录题号、调用方 request_id、是否命中缓存、完整耗时、结果是否完整、正确来源 Top-20/Top-5 命中、必要条件/数字/例外核对、权限/版本判定和最终结论。失败样本保留，不用重试成功替代首次失败。
